export type TopicPoint = {
  id: string;
  title: string;
  description: string;
};

export type AwarenessExperience = {
  id: string;
  label: string;
};

export type CopingPractice = {
  id: string;
  title: string;
  description: string;
};

export type PageCtaLink = {
  label: string;
  href: string;
};
